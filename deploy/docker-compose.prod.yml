# ============================================================
# docker-compose.prod.yml: orquestracao de producao do MeasureSoftGram.
#
# Topologia: proxy nginx interno publica :80 no host e roteia /api,
# /swagger, /admin, /static -> service:8080 e / -> front:3000. db e
# service NAO publicam porta (so rede interna do compose).
#
# Deploy: PULL das imagens buildadas no CI (a box so puxa, nao builda).
#   docker compose -f deploy/docker-compose.prod.yml pull
#   docker compose -f deploy/docker-compose.prod.yml up -d
#
# Variaveis lidas do ambiente / arquivo .env ao lado deste compose:
#   DOCKERHUB_USERNAME  -> namespace das imagens service/front
#   SERVICE_IMAGE_TAG   -> tag da imagem do service  (default: latest)
#   FRONT_IMAGE_TAG     -> tag da imagem do front    (default: latest)
# Segredos das apps vivem em ./env-vars/*.env (gitignored), provisionados
# na box, nunca no repo.
# ============================================================

# Nome FIXO do projeto compose. Sem isso, o project name vira o basename do
# diretorio onde o compose roda, que mudou entre o deploy antigo (rodava do
# checkout do runner -> "deploy") e o novo (~/msgram-deploy). Project name
# diferente = compose cria stack/volume PARALELOS e colide nos container_name
# fixos (msgram_db etc.). Pinar em "deploy" faz todo deploy (Service e Front)
# adotar a stack existente e reusar o volume com dados (deploy_service_postgres_data).
name: deploy

volumes:
  service_postgres_data:
  grafana_data:

networks:
  msgram:
    driver: bridge

services:
  db:
    container_name: msgram_db
    image: postgres:18-alpine
    volumes:
      # postgres 18+ guarda os dados num subdir version-specific e exige o mount
      # na raiz /var/lib/postgresql (a imagem rejeita o mount direto em /data).
      - service_postgres_data:/var/lib/postgresql
    env_file:
      - ./env-vars/.postgres.env
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - msgram
    restart: unless-stopped

  service:
    container_name: msgram_service
    image: ${DOCKERHUB_USERNAME}/service:${SERVICE_IMAGE_TAG:-latest}
    env_file:
      # .service.env deve conter entre outros:
      #   GRAFANA_PUBLIC_URL=https://<dominio>/grafana
      #   GRAFANA_USERNAME e GRAFANA_PASSWORD (mesmos do .grafana.env)
      - ./env-vars/.service.env
      - ./env-vars/.postgres.env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.production
      # Deploy de demonstracao: popula dashboards com dados falsos.
      # Setar "FALSE" (ou remover) em producao real com dados de verdade.
      CREATE_FAKE_DATA: "TRUE"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      # /swagger e AllowAny -> 200 quando o app esta de pe.
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/swagger/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 40s
    networks:
      - msgram
    restart: unless-stopped

  grafana:
    container_name: msgram_grafana
    image: grafana/grafana:latest
    volumes:
      - grafana_data:/var/lib/grafana
      - ${GRAFANA_PERSIST_DIR:-/home/lappis/msgram-grafana}/provisioning:/etc/grafana/provisioning
      - ${GRAFANA_PERSIST_DIR:-/home/lappis/msgram-grafana}/dashboards:/etc/grafana/dashboards
    env_file:
      # .postgres.env alimenta o datasource do Grafana (mesmo banco do service)
      - ./env-vars/.postgres.env
      # .grafana.env deve conter: GF_SECURITY_ADMIN_USER, GF_SECURITY_ADMIN_PASSWORD
      - ./env-vars/.grafana.env
    environment:
      GF_SERVER_ROOT_URL: '%(protocol)s://%(domain)s/grafana'
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
      GF_SECURITY_ALLOW_EMBEDDING: "true"
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_INSTALL_PLUGINS: volkovlabs-echarts-panel
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: volkovlabs-echarts-panel
    depends_on:
      db:
        condition: service_healthy
    networks:
      - msgram
    restart: unless-stopped

  front:
    container_name: msgram_front
    image: ${DOCKERHUB_USERNAME}/front:${FRONT_IMAGE_TAG:-latest}
    # env do front (ex: URL publica da API) provisionado pelo time de Front
    # em ./env-vars/.front.env se necessario.
    depends_on:
      - service
    networks:
      - msgram
    restart: unless-stopped

  proxy:
    container_name: msgram_proxy
    image: nginx:1.27-alpine
    # Proxy INTERNO da stack: publica :80 do host. O proxy externo da box
    # (openresty) fica na frente e encaminha o dominio pra esta :80.
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - service
      - front
    networks:
      - msgram
    restart: unless-stopped
